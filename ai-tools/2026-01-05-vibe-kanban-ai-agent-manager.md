# Vibe Kanban - AI 编程代理的"赛博包工头"

> 精读日期：2026-01-05
> 项目地址：https://github.com/BloopAI/vibe-kanban
> 官网：https://www.vibekanban.com/

## 📖 快速概览

| 项目 | 内容 |
|------|------|
| **标题** | Vibe Kanban - AI 编程代理的"赛博包工头" |
| **作者** | BloopAI 团队 |
| **类型** | 开源工具（Rust + TypeScript） |
| **核心主题** | 用看板管理多个 AI 编程助手，实现并行开发 |
| **目标读者** | 重度使用 AI 编程工具的开发者 |
| **阅读价值** | ⭐⭐⭐⭐⭐ 解决 Vibe Coding 时代的核心痛点 |

> **一句话总结**：Vibe Kanban 把 AI 编程从"对话模式"升级为"任务管理模式"，让你像管理团队一样管理多个 AI 助手。

---

## 🗺️ 知识地图

### 核心问题链

```
问题：AI 编程工具越来越强，为什么我们反而更累了？
    ↓
原因：我们想让 AI 同时干多件事，但管理 AI 比自己写代码还累
    ↓
解法：需要一个"包工头"系统来调度这些 AI "工人"
    ↓
方案：Vibe Kanban = 看板 + Git Worktree + 多 Agent 调度
```

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    Vibe Kanban (包工头)                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                 可视化看板界面                         │  │
│  │   [Todo] → [In Progress] → [In Review] → [Done]      │  │
│  └──────────────────────────────────────────────────────┘  │
│                            │                                │
│                    任务调度 & 状态追踪                       │
│                            │                                │
│  ┌─────────────┬─────────────┬─────────────┬────────────┐  │
│  │ Worktree A  │ Worktree B  │ Worktree C  │ Worktree D │  │
│  │ /fix-bug    │ /new-feat   │ /refactor   │ /test      │  │
│  │             │             │             │            │  │
│  │ Claude Code │ Gemini CLI  │ Codex       │ Cursor CLI │  │
│  └─────────────┴─────────────┴─────────────┴────────────┘  │
│                            │                                │
│                    Git 主仓库 (main)                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📚 深度讲解

### 1. 核心痛点：为什么需要 Vibe Kanban？

**问题本质**：传统 AI 编程工具是"对话模式"——一问一答，串行执行。

| 传统模式 | Vibe Kanban 模式 |
|----------|------------------|
| 一个窗口一个 AI | 多个 AI 并行工作 |
| 共享同一份代码 | 每个任务独立工作空间 |
| 手动切换任务 | 看板可视化管理 |
| 代码容易冲突 | Git Worktree 隔离 |

**类比理解**：
- 传统模式 = 你是唯一的项目经理，只能盯着一个员工干活
- Vibe Kanban = 你是包工头，有一个调度中心，可以同时指挥多个工人

---

### 2. 核心技术：Git Worktree 隔离

**分支 vs Worktree 的区别**：

| 维度 | 多分支 | Git Worktree |
|------|--------|--------------|
| 文件系统 | 共享同一目录 | 每个分支独立目录 |
| 切换成本 | 需要 checkout | 无需切换，直接访问 |
| 并行编辑 | 同一文件会冲突 | 物理隔离，互不干扰 |
| 运行环境 | 共享 | 可独立启动 Dev Server |

**Worktree 工作原理**：

```bash
# 传统方式：切换分支
git checkout feature-a  # 整个目录变成 feature-a 的代码
git checkout feature-b  # 整个目录变成 feature-b 的代码（覆盖了）

# Worktree 方式：并行存在
git worktree add ../worktrees/feature-a feature-a
git worktree add ../worktrees/feature-b feature-b
# 现在有两个独立目录，可以同时编辑！
```

**Vibe Kanban 的实现**：
1. 创建任务时，自动创建新的 Worktree
2. 每个 AI Agent 在自己的 Worktree 里工作
3. 完成后，通过 diff 审查，再 merge 回主分支

---

### 3. 多 Agent 调度策略

**Agent 特点对比**：

| Agent | 优势 | 适合任务 |
|-------|------|----------|
| **Claude Code** | 逻辑强、上下文理解好 | 复杂业务逻辑、架构设计 |
| **Gemini CLI** | 速度快、长文本处理 | 文档生成、简单重构 |
| **Codex/Copilot** | 代码补全精准 | 单文件编辑、API 调用 |
| **Cursor CLI** | IDE 集成好 | 需要上下文的修改 |

**调度策略示例**：

```
任务池：
├── [复杂] 重构支付模块 → Claude Code（需要理解业务）
├── [简单] 写单元测试 → Gemini CLI（批量生成）
├── [中等] 修复 3 个 Bug → Codex（精准修改）
└── [文档] 更新 README → Gemini CLI（长文本）
```

**关键洞见**：Vibe Kanban 不是让某个 AI 变聪明，而是让你能**按需分配**最合适的 AI。

---

### 4. 工作流程详解

**完整生命周期**：

```
1. Todo（待办）
   └── 创建任务卡片，描述需求
   └── 选择 AI Agent
   └── 系统自动创建 Worktree

2. In Progress（进行中）
   └── AI 在独立 Worktree 中工作
   └── 实时查看终端输出
   └── 可同时运行多个任务

3. In Review（审查中）
   └── AI 完成后进入此状态
   └── 查看 diff（代码变更）
   └── 可启动 Dev Server 预览效果
   └── 修改/批准/拒绝

4. Done（完成）
   └── 一键 Merge 到主分支
   └── 自动创建 PR（可选）
   └── 清理 Worktree
```

**审查环节的价值**：

| 传统方式 | Vibe Kanban |
|----------|-------------|
| AI 直接改主分支 | 先在独立空间完成 |
| 出错要手动回滚 | 不满意直接丢弃 |
| 看不清改了什么 | Diff 一目了然 |
| 无法预览效果 | 独立 Dev Server |

---

### 5. MCP 集成能力

**双向集成**：

```
┌─────────────────────────────────────────────────────┐
│                   MCP 生态系统                       │
│                                                     │
│  ┌─────────────┐      ┌─────────────────────────┐  │
│  │ Claude      │      │     Vibe Kanban         │  │
│  │ Desktop     │ ───▶ │  (作为 MCP Server)       │  │
│  │ (MCP Client)│      │  - 接收任务创建请求      │  │
│  └─────────────┘      │  - 返回任务状态          │  │
│                       └─────────────────────────┘  │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │     Vibe Kanban (作为 MCP Client)            │   │
│  │     ↓                                        │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │MCP Server│  │MCP Server│  │MCP Server│   │   │
│  │  │(GitHub)  │  │(Database)│  │(Sentry)  │   │   │
│  │  └──────────┘  └──────────┘  └──────────┘   │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**实际意义**：
1. **作为 MCP Server**：可以从 Claude Desktop 直接创建 Vibe Kanban 任务
2. **作为 MCP Client**：可以让 AI Agent 使用各种 MCP 工具（GitHub、数据库等）

---

## ✅ 行动清单

### 立即可做（今天）

- [ ] 安装体验：`npx vibe-kanban`
- [ ] 创建第一个任务，感受 Worktree 隔离

### 短期实践（本周）

- [ ] 尝试同时运行 2-3 个 AI Agent
- [ ] 对比不同 Agent 处理同类任务的效果

### 长期提升（持续）

- [ ] 建立个人的 Agent 调度策略（什么任务用什么 Agent）
- [ ] 探索 MCP 集成，扩展工作流

---

## 📋 一页纸总结

### 核心要点（5 条）

1. **Vibe Kanban 不是 AI 编程工具，是 AI 编程工具的"包工头"**
2. **Git Worktree 实现物理隔离，让多个 AI 真正并行工作**
3. **可视化看板 + Diff 审查 = 掌控感 + 质量保证**
4. **不绑定特定 AI，按任务难度分配最合适的 Agent**
5. **MCP 双向集成，可融入更大的 AI 工作流生态**

### 金句摘录

> "我们缺的不是更聪明的 AI，而是一个能管理这些 AI 的系统。"

> "Vibe kanban is the biggest increase I've had in productivity since cursor." — Luke Harries, Eleven Labs

### 与 MCP 的关联

| 概念 | MCP | Vibe Kanban |
|------|-----|-------------|
| 定位 | AI 与外部世界的连接协议 | AI Agent 的调度系统 |
| 关系 | 标准/协议 | 实现/应用 |
| 集成 | 定义 Server/Client | 既是 Server 也是 Client |

---

## 延伸阅读

- [Vibe Kanban GitHub](https://github.com/BloopAI/vibe-kanban)
- [Git Worktree 官方文档](https://git-scm.com/docs/git-worktree)
- [MCP 协议规范](https://modelcontextprotocol.io/)
