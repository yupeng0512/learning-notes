# Cursor 多 Agent 并发编码：数百个 Agent 协同工作数周的经验

> 学习日期：2026-01-16
> 来源：Cursor 团队内部分享
> 标签：#MultiAgent #Cursor #并发编程 #Agent协调 #架构设计

## 📖 快速概览

| 项目 | 内容 |
|------|------|
| **标题** | Cursor 多 Agent 并发编码：如何让数百个 Agent 协同工作数周 |
| **作者** | Cursor 团队（AI 编程工具先驱） |
| **类型** | 技术博客（前沿实践经验分享） |
| **核心主题** | 探索多 Agent 并发协作的架构演进与实战经验 |
| **目标读者** | AI Agent 开发者、系统架构师、对 AI 自主编程感兴趣的技术人员 |
| **阅读价值** | ⭐⭐⭐⭐⭐ 极其稀缺的一手实战经验，揭示了 Agent 大规模协作的核心挑战与解法 |

> **一句话总结**：通过"规划者-执行者"分层架构，Cursor 实现了上百个 Agent 在同一代码库协同工作数周，这标志着**从"单 Agent 辅助"到"多 Agent 自主"的范式跃迁**。

---

## 🗺️ 知识地图

### 文章结构

```
┌─────────────────────────────────────────────────────────────────┐
│           Cursor 多 Agent 并发编码探索                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐                                               │
│  │ 问题起点     │  单 Agent 局限 → 需要并行化                    │
│  └──────┬───────┘                                               │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────────────────────────────────────────────┐       │
│  │               协调方案演进（三次迭代）                │       │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐      │       │
│  │  │ 方案一    │→│ 方案二    │→│ 方案三    │      │       │
│  │  │ 扁平+锁   │  │ 乐观并发  │  │ 规划-执行 │      │       │
│  │  │   ❌      │  │   ⚠️      │  │   ✅      │      │       │
│  │  └────────────┘  └────────────┘  └────────────┘      │       │
│  └──────────────────────────────────────────────────────┘       │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────┐                                               │
│  │ 实战验证     │  FastRender / Solid→React / 视频渲染          │
│  └──────┬───────┘                                               │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────┐                                               │
│  │ 关键经验     │  模型选择 / 减法思维 / 结构平衡 / 提示词       │
│  └──────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📚 核心问题：单个 Agent 的局限

如今的 Agent 在执行专注的小任务时表现不错，但在复杂项目上却显得缓慢。自然而然的下一步是并行运行多个 Agent，但要搞清楚如何协调它们却并不容易。

最初的直觉是，事先规划会过于僵化。推进一个大型项目的路径往往并不明确，合理的工作拆分在一开始也并不清晰。于是从动态协调入手，让 Agent 根据其他 Agent 此刻正在做的事情来决定自己的下一步。

---

## 🔄 协调方案演进：三次迭代

### 方案一：扁平结构 + 锁机制 ❌

**设计思路**：
- 所有 agent 具有同等地位，通过共享文件自行协同
- 每个 agent 检查其他 agent 在做什么、认领任务并更新状态
- 使用锁机制防止两个 agent 抢占同一任务

**失败原因**：
1. **锁持有过久**：Agent 不像程序，操作时间不可预测
2. **忘记释放锁**：Agent 可能中途失败或走神
3. **吞吐量坍塌**：20 个 Agent 的效率等于 2-3 个
4. **系统脆弱**：Agent 可能在持有锁时失败、尝试获取已持有的锁

### 方案二：乐观并发控制 ⚠️

**设计思路**：
- Agent 可以自由读取状态
- 如果状态已变化则写入失败，需要重试
- 更简单、更健壮

**新问题**：
- 在没有层级结构的情况下，Agent 变得**非常规避风险**
- 它们回避困难任务，转而做小而安全的修改
- 没有任何一个 Agent 承担起解决难题或端到端实现的责任
- 结果是工作长时间空转，却没有实质性进展

### 方案三：规划者-执行者架构 ✅

**设计思路**：将不同角色拆分开来，搭建职责清晰的流水线

```
┌─────────────────────────────────────────────────────────┐
│                     评审 Agent                         │
│                   (周期性检查)                          │
│                        ▲                               │
│                        │                               │
│  ┌─────────────────────┴─────────────────────────┐     │
│  │               规划者层 (Planners)              │     │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐    │     │
│  │  │主规划者   │──│子规划者A │──│子规划者B │    │     │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘    │     │
│  │       │             │             │          │     │
│  │  创建任务       创建任务       创建任务        │     │
│  └───────┼─────────────┼─────────────┼──────────┘     │
│          │             │             │                │
│          ▼             ▼             ▼                │
│  ┌────────────────────────────────────────────────┐   │
│  │              执行者层 (Workers)                 │   │
│  │   ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐   │   │
│  │   │W1  │ │W2  │ │W3  │ │W4  │ │W5  │ │...│   │   │
│  │   └────┘ └────┘ └────┘ └────┘ └────┘ └────┘   │   │
│  │   专注执行，不协调，完成即提交                   │   │
│  └────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**三种角色的职责**：

| 角色 | 职责 | 特点 |
|------|------|------|
| **规划者 (Planners)** | 持续探索代码库并创建任务，可派生子规划者 | 递归、并行、全局视野 |
| **执行者 (Workers)** | 领取任务并专注于把任务完成到底 | 不与其他执行者协调，只管执行 |
| **评审 Agent** | 在每个周期结束时判断是否继续 | 下一轮从干净状态重新开始 |

**为什么有效**：
1. **责任清晰**：规划者负责"做什么"，执行者负责"怎么做"
2. **消除协调开销**：执行者之间不需要互相知道对方
3. **可扩展**：规划者可递归派生，执行者可无限扩展
4. **容错性强**：单个执行者失败不影响整体

---

## 📊 实战验证：震撼的数据

### 案例一：从零构建浏览器（FastRender）

- **持续时间**：将近一周
- **代码规模**：在 1,000 个文件中写出超过 100 万行代码
- **并发规模**：成百上千个 worker 并发运行
- **冲突率**：向同一个分支推送代码，几乎没有冲突
- **GitHub**: https://github.com/wilsonzlin/fastrender

### 案例二：Cursor 代码库迁移 Solid → React

- **持续时间**：3 周多
- **代码变更**：+266K/-193K
- **评估**：认为有可能合并这次大规模改动

### 案例三：视频渲染优化

- **性能提升**：通过高效的 Rust 实现，让视频渲染速度提升 25 倍
- **新功能**：平滑缩放和平移能力（弹簧过渡和运动模糊效果）
- **状态**：代码已合并，即将上线生产环境

### 其他进行中的实验

| 项目 | 提交次数 | 代码行数 |
|------|----------|----------|
| Java LSP | 7.4K 次提交 | 55 万行 |
| Windows 7 模拟器 | 14.6K 次提交 | 120 万行 |
| Excel | 12K 次提交 | 160 万行 |

---

## 💡 关键经验总结

### 经验一：模型选择至关重要

| 模型 | 特点 | 适合角色 |
|------|------|----------|
| **GPT-5.2 系列** | 更能遵循指令、保持专注、避免偏离，实现更精确完整 | 规划者、长时间自主工作 |
| **Opus 4.5** | 倾向于更早结束、走捷径，更快把控制权交还用户 | 短期任务 |
| **GPT-5.1-codex** | 专门为编码训练 | 执行者 |

**关键洞见**：针对每个角色选择最适合的模型，而不是依赖单一通用模型。

### 经验二：减法比加法更重要

**案例**：
- 最初设计了"集成者"角色来解决质量控制和冲突
- 后来发现它制造的瓶颈多于解决的问题
- 去掉后，让 Worker 自行处理冲突，系统反而更好

**深层原因**：每增加一个协调层，就增加了通信开销、决策延迟、单点故障风险。

> **金句**："最好的系统往往比你想的更简单。"

### 经验三：结构化程度的平衡

```
        ◄─────────────────────────────────────────►
        太少                                    太多
        ┌───────────────────────────────────────────┐
        │ 互相冲突                         系统脆弱 │
        │ 重复劳动           最佳区间       难以适应 │
        │ 不断偏离           ◀──▶         过度僵化 │
        └───────────────────────────────────────────┘
```

### 经验四：提示词比框架更重要

- 系统行为很大程度上取决于如何设计提示
- 让 Agent 良好协作、避免异常行为、长时间保持专注，需要大量实验
- 运行框架和模型本身固然重要，但**提示词更重要**

---

## ⚠️ 当前局限

| 问题 | 性质 | 可能的解决方向 |
|------|------|----------------|
| 规划者不会自动醒来 | 工程问题 | 事件驱动架构 |
| Agent 运行时间过长 | 技术+模型问题 | 更好的终止条件判断 |
| 需要定期重启对抗漂移 | 根本性挑战 | 模型长期记忆能力提升 |
| 思维视野狭窄 | 根本性挑战 | 更强的自我反思能力 |

---

## 📋 核心要点总结

1. **单 Agent 有天花板** → 复杂项目需要多 Agent 并行
2. **扁平结构会导致责任模糊** → 没人愿意承担困难任务
3. **规划者-执行者分层是有效解法** → 认知负荷分离 + 松耦合
4. **减法比加法更重要** → 协调层可能是瓶颈而非助力
5. **提示词比框架更重要** → 系统行为取决于 prompt 设计

---

## 💬 金句收录

> "对于'能否通过向问题投入更多 Agent 来扩展自主编码能力'，答案比预期更乐观。"

> "最好的系统往往比你想的更简单。"

> "不同模型在不同角色上各有所长——不是依赖单一通用模型。"

> "运行框架和模型本身固然重要，但提示词更重要。"

---

## 🎯 设计原则提炼

| 原则 | 说明 |
|------|------|
| **单一职责** | 每种 Agent 只做一件事 |
| **层次分明** | 规划和执行明确分层 |
| **松耦合** | 执行者之间无需协调 |
| **可递归** | 复杂问题可分解为子问题 |
| **周期重置** | 定期清理上下文保持新鲜 |

---

## 🔗 相关链接

- [FastRender GitHub](https://github.com/wilsonzlin/fastrender) - Cursor 多 Agent 构建的浏览器
- [Aero (Windows 7 模拟器)](https://github.com/wilsonzlin/aero)
- [Indonesia (Java LSP)](https://github.com/wilson-anysphere/indonesia)
- [Formula (Excel)](https://github.com/wilson-anysphere/formula)
