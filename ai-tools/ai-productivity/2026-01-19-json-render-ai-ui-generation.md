---
title: "json-render - AI 生成 UI 的约束式革命"
date: 2026-01-19
tags: [AI, Frontend, UI Generation, Vercel, DSL]
category: ai-tools
subcategory: ai-productivity
source: https://github.com/vercel-labs/json-render
---

# json-render - AI 生成 UI 的约束式革命

> 📖 来源：[Vercel Labs - json-render](https://github.com/vercel-labs/json-render)
> 📅 学习日期：2026-01-19
> 🏷️ 分类：AI 工具与效率 / AI 生产力

---

## 根节点命题

> **"AI → JSON → UI"：用结构化中间层将 AI 的"生成自由度"转化为"填空题"，从源头消除不可控性**

**为什么这是根节点**：json-render 所有设计都可以从这个架构决策推导出来——
- 为什么安全？因为 AI 只能在 Catalog 定义的组件里"填空"，无法执行任意代码
- 为什么可预测？因为 JSON Schema 约束了输出结构，不存在"格式意外"
- 为什么能活过来？因为 JSON 支持 `valuePath` 数据绑定，渲染时动态取值
- 为什么能导出代码？因为 JSON 是结构化的，可以无损翻译成任意框架代码

---

## 表示空间

> **理解 AI 生成 UI 问题的最小维度**

| 维度 | 传统 AI 生成 | json-render 方案 |
|------|-------------|------------------|
| **生成对象** | 直接生成代码（HTML/JSX） | 生成 JSON 描述 |
| **自由度** | 完全自由（可写任意代码） | 受限于 Catalog 定义 |
| **安全边界** | 无（可能注入恶意脚本） | 有（只能使用预定义组件） |
| **数据交互** | 静态（写死的假数据） | 动态（valuePath 绑定真实数据） |
| **可导出性** | 低（格式不一致） | 高（结构化可翻译） |

---

## 推论展开

```
根节点：AI → JSON → UI（结构化中间层）
│
├─ 推论1：Catalog 定义组件白名单 → 彻底的安全"紧箍咒"
│   └─ 应用：AI 只能用预定义的 Card/Button/Chart，无法注入恶意代码
│
├─ 推论2：Zod Schema 约束 props → 输出 100% 可预测
│   └─ 应用：每个组件的属性都有类型定义，AI 填错了直接报错
│
├─ 推论3：valuePath 数据绑定 → 界面"活"过来
│   └─ 应用：JSON 中写 `"valuePath": "/revenue"`，渲染时自动拉取真实数据
│
├─ 推论4：Action Schema → 按钮能触发真实业务逻辑
│   └─ 应用：AI 声明意图（如 "export_report"），由开发者实现具体逻辑
│
└─ 推论5：JSON 结构化 → Code Export 无损导出
    └─ 应用：一键翻译成 Next.js 代码，粘贴即用
```

---

## 泛化模式

> **"用约束消除不可控"这个模式可以迁移到哪些场景？**

| 原场景 | 迁移场景 | 如何应用 |
|--------|----------|----------|
| AI 生成 UI → JSON 中间层 | **AI 生成 SQL** | 定义允许的表/字段白名单，AI 只能在范围内组装查询 |
| AI 生成 UI → JSON 中间层 | **AI 生成 API 调用** | 定义允许的 endpoint 和参数 schema，AI 填参数 |
| AI 生成 UI → JSON 中间层 | **AI 生成工作流** | 定义允许的 action 列表，AI 编排顺序和参数 |
| AI 生成 UI → JSON 中间层 | **AI 生成配置文件** | 定义 config schema，AI 在约束内生成 YAML/JSON |

**泛化公式**：任何"AI 生成 X 不可控"的场景，都可以通过引入"结构化中间层 + Schema 约束"来解决

---

## 关键概念

> **只保留理解根节点必需的概念**

| 概念 | 定义 | 与根节点的关系 |
|------|------|----------------|
| **Catalog** | 组件白名单，定义 AI 可用的组件及其 props schema | 约束的来源——AI 只能在这里"填空" |
| **JSON Tree** | AI 生成的结构化 UI 描述 | 中间层——承上启下的桥梁 |
| **Renderer** | 将 JSON Tree 渲染为 React 组件 | 消费端——把描述变成真实 UI |
| **valuePath** | JSON 中的数据绑定路径 | 让静态描述"活"过来的关键 |
| **Action** | AI 声明的用户意图（如导出、刷新） | 连接 UI 和业务逻辑的桥梁 |

---

## 核心架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     json-render 架构                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ① 定义阶段（开发者）                                            │
│     ├─ createCatalog() → 定义组件白名单 + props schema          │
│     └─ registry → 实现每个组件的 React 渲染逻辑                   │
│                                                                 │
│  ② 生成阶段（AI）                                                │
│     ├─ 用户输入 prompt: "Create a revenue dashboard"            │
│     ├─ AI 根据 Catalog 约束，输出 JSON Tree                      │
│     └─ 流式返回，边生成边渲染                                     │
│                                                                 │
│  ③ 渲染阶段（运行时）                                            │
│     ├─ DataProvider → 注入真实业务数据                           │
│     ├─ ActionProvider → 注册 action 处理函数                     │
│     └─ Renderer → 遍历 JSON Tree，渲染 React 组件                │
│                                                                 │
│  ④ 导出阶段（可选）                                              │
│     └─ Code Export → JSON → Next.js 代码                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 行动清单

| 推论 | 行动 | 预计时间 |
|------|------|----------|
| Catalog 约束消除安全风险 | 评估现有 AI 生成 UI 方案的安全边界 | ⏱️ 30 分钟 |
| Schema 保证可预测 | 学习 Zod schema 定义，理解类型约束 | ⏱️ 1 小时 |
| valuePath 数据绑定 | 在 Side Project 中尝试 json-render | ⏱️ 2 小时 |
| 泛化到其他场景 | 思考团队中哪些 AI 生成场景可以用类似方案 | ⏱️ 持续 |

---

## 个人思考

**与 OctoCodingBench 的关联**：

json-render 的设计理念和 OctoCodingBench 评测的核心问题高度一致：
- OctoCodingBench 发现：AI 在复杂约束下 ISR 很低（容易违规）
- json-render 的解法：**降低 AI 的自由度**，把"写代码"变成"填 JSON"

这是一种**主动降维**的策略：
- 不是让 AI 变得更"聪明"去遵守规则
- 而是让 AI 能做的事变少，规则自然就能遵守

**类比**：
- 传统 AI 生成 UI = 让实习生自由发挥写代码（不可控）
- json-render = 让实习生填表格（表格设计好了，填错也有限）

---

## 延伸阅读

- [json-render GitHub](https://github.com/vercel-labs/json-render) - 官方仓库，6.7k+ Stars
- [Zod](https://zod.dev/) - TypeScript-first schema 验证库
- [OctoCodingBench](../agent-architecture/2026-01-19-octocodingbench-process-evaluation.md) - AI 过程合规评测（相关笔记）

---

## 补充材料

<details>
<summary>📋 代码示例：三步上手</summary>

**Step 1: 定义 Catalog**
```typescript
import { createCatalog } from '@json-render/core';
import { z } from 'zod';

const catalog = createCatalog({
  components: {
    Card: {
      props: z.object({ title: z.string() }),
      hasChildren: true,
    },
    Metric: {
      props: z.object({
        label: z.string(),
        valuePath: z.string(),  // 数据绑定
        format: z.enum(['currency', 'percent', 'number']),
      }),
    },
  },
  actions: {
    export_report: { description: 'Export dashboard to PDF' },
  },
});
```

**Step 2: 注册组件实现**
```typescript
const registry = {
  Card: ({ element, children }) => (
    <div className="card">
      <h3>{element.props.title}</h3>
      {children}
    </div>
  ),
  Metric: ({ element }) => {
    const value = useDataValue(element.props.valuePath);
    return <div className="metric">{format(value)}</div>;
  },
};
```

**Step 3: 渲染**
```typescript
import { Renderer, useUIStream } from '@json-render/react';

function Dashboard() {
  const { tree } = useUIStream({ api: '/api/generate' });
  return <Renderer tree={tree} components={registry} />;
}
```

</details>

<details>
<summary>🔧 高级特性：条件可见性 & 富动作</summary>

**条件可见性**（根据数据动态显示/隐藏）：
```json
{
  "type": "Alert",
  "props": { "message": "Error occurred" },
  "visible": {
    "and": [
      { "path": "/form/hasError" },
      { "not": { "path": "/form/errorDismissed" } }
    ]
  }
}
```

**富动作**（带确认对话框）：
```json
{
  "type": "Button",
  "props": {
    "label": "Refund Payment",
    "action": {
      "name": "refund",
      "params": {
        "paymentId": { "path": "/selected/id" },
        "amount": { "path": "/refund/amount" }
      },
      "confirm": {
        "title": "Confirm Refund",
        "message": "Refund ${/refund/amount} to customer?",
        "variant": "danger"
      }
    }
  }
}
```

</details>

---

## 金句摘录

> "AI 不再负责'造房子'（写逻辑代码），只负责'画图纸'（输出结构化的 JSON 数据）。"

> "把不可控的生成式 AI，变成了精准的填空题。"

> "JSON 就像是一张标准化的'填空题'。AI 只能在格子里填内容，而不能在纸上乱涂乱画。"
