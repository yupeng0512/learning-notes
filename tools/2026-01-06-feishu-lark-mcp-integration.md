# 飞书 MCP 接入与使用指南

> 📅 学习日期：2026-01-06
> 🔗 参考链接：https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/mcp_integration/mcp_installation
> 🏷️ 分类：工具

## 一句话总结

飞书 MCP 让你的本地 AI Agent（Cursor/Claude Code/Gemini CLI）能够直接读写飞书文档、操作多维表格、发送消息，是个人办公自动化的利器。

---

## 核心要点

1. **飞书 MCP 是 AI Agent 调用飞书 API 的桥梁**，让 AI 能操作文档、表格、消息
2. **仅支持本地调用**，适用于 Cursor、Claude Code、Gemini CLI 等本地 AI Agent
3. **需要创建飞书应用**获取 App ID/Secret，并配置对应 API 权限
4. **提供 18 个工具**，覆盖云文档、多维表格、消息三大模块
5. **文档只能读取纯文本**，图片/评论需要额外 API 调用

---

## 知识地图

```
┌─────────────────────────────────────────────────────────────┐
│               飞书 MCP 接入指南                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1. 核心概念                                          │   │
│  │    • MCP = Model Context Protocol                    │   │
│  │    • 飞书 MCP = AI Agent 调用飞书 API 的桥梁         │   │
│  │    • 限制：仅支持本地调用，不支持服务端部署          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 2. 准备工作                                          │   │
│  │    • 创建飞书企业应用                                │   │
│  │    • 获取 App ID + App Secret                        │   │
│  │    • 配置 API 权限                                   │   │
│  │    • 设置重定向 URL                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 3. 安装配置                                          │   │
│  │    • Node.js >= v20.0.0                              │   │
│  │    • npx 安装 @larksuiteoapi/lark-mcp               │   │
│  │    • 配置 JSON（适用于所有 AI Agent）                │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 4. 功能清单（三大模块）                              │   │
│  │    • 云文档与知识库（6 个工具）                      │   │
│  │    • 多维表格 Bitable（7 个工具）                    │   │
│  │    • 消息与群组 IM（5 个工具）                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 关键概念

| 概念 | 定义 | 作用 |
|------|------|------|
| **MCP** | Model Context Protocol，模型上下文协议 | 让 AI Agent 能够调用外部工具的标准协议 |
| **飞书 MCP** | 飞书官方提供的 MCP 实现 | 让 AI Agent 能操作飞书文档、表格、消息 |
| **App ID / App Secret** | 飞书应用凭证 | 用于身份认证，授权 MCP 调用飞书 API |
| **user_access_token** | 用户访问令牌 | 以用户身份调用 API，获取用户有权限的资源 |

---

## 准备工作

### 1. 创建飞书应用

```
1. 访问飞书开放平台 → 创建企业自建应用
2. 设置应用名称、描述、图标
3. 在「凭证与基础信息」页面获取 App ID 和 App Secret
4. 在「权限管理」页面添加所需 API 权限
5. 在「安全设置」添加重定向 URL：http://localhost:3000/callback
```

### 2. 权限配置

| 功能 | 需要的权限 |
|------|-----------|
| 读取文档 | `docx:document`、`wiki:wiki:readonly` |
| 编辑文档 | `docx:document`（写权限） |
| 操作多维表格 | `bitable:app` |
| 发送消息 | `im:message`、添加「机器人」能力 |
| 搜索文档 | `drive:drive` |

**批量开通权限**（复制到权限管理页面）：
```
im:chat:create, im:chat, im:message, wiki:wiki, wiki:wiki:readonly, docx:document, bitable:app, drive:drive
```

---

## 安装配置

### 安装命令

```bash
# 首次登录授权（需要 Node.js >= v20.0.0）
npx -y @larksuiteoapi/lark-mcp login -a <appId> -s <appSecret>
```

### MCP 配置 JSON

```json
{
  "mcpServers": {
    "lark-mcp": {
      "command": "/Users/xxx/.nvm/versions/node/v20.xx.xx/bin/npx",
      "args": [
        "-y",
        "@larksuiteoapi/lark-mcp",
        "mcp",
        "-a", "<你的appId>",
        "-s", "<你的appSecret>",
        "--oauth"
      ],
      "timeout": 30000
    }
  }
}
```

### 配置文件位置

| AI Agent | 配置文件位置 |
|----------|-------------|
| Cursor | `.cursor/mcp.json` 或全局配置 |
| Claude Code | `~/.claude/claude_desktop_config.json` |
| Cline (VSCode) | 插件设置中配置 |

---

## 功能清单

### 模块一：云文档与知识库（6 个工具）

| 工具名称 | 功能 | 使用场景 |
|----------|------|----------|
| `docx_builtin_import` | 导入 Markdown 生成飞书文档 | 把 AI 生成的内容保存到飞书 |
| `docx_builtin_search` | 搜索飞书文档 | 找到相关文档 |
| `docx_v1_document_rawContent` | 获取文档纯文本内容 | 让 AI 理解文档内容 |
| `drive_v1_permissionMember_create` | 添加文档协作者 | 自动分享文档 |
| `wiki_v1_node_search` | 搜索知识库节点 | 在知识库中查找 |
| `wiki_v2_space_getNode` | 获取节点详情 | 获取文档标题、层级等 |

### 模块二：多维表格 Bitable（7 个工具）

| 工具名称 | 功能 | 使用场景 |
|----------|------|----------|
| `app_create` | 创建多维表格应用 | 新建数据管理表 |
| `appTable_create` | 创建数据表 | 定义表结构 |
| `appTable_list` | 获取数据表列表 | 查看有哪些表 |
| `appTableField_list` | 获取字段列表 | 了解表结构 |
| `appTableRecord_create` | 创建记录 | 插入数据 |
| `appTableRecord_search` | 搜索记录 | 查询数据 |
| `appTableRecord_update` | 更新记录 | 修改数据 |

### 模块三：消息与群组 IM（5 个工具）

| 工具名称 | 功能 | 使用场景 |
|----------|------|----------|
| `chat_create` | 创建群组 | 自动建群 |
| `chat_list` | 获取群组列表 | 查看所在群 |
| `chatMembers_get` | 获取群成员 | 了解群成员 |
| `message_create` | 发送消息 | 自动发送通知 |
| `message_list` | 获取聊天记录 | 读取历史消息 |

---

## 功能限制与解决办法

### 限制

`docx_v1_document_rawContent` 只能读取**纯文本**，无法获取：
- 文档内的图片
- 侧边评论内容

### 解决办法

让 AI 调用额外的飞书 API：

```
方案：在工作流中让 AI 主动调用飞书 API
1. 获取文档 block 结构 → 找到图片 block
2. 调用图片下载 API → 获取图片内容
3. 调用评论 API → 获取评论内容
4. 整合所有信息 → AI 完整理解文档
```

---

## 实用技巧

| 技巧 | 应用场景 | 效果 |
|------|----------|------|
| 贴飞书文档链接给 AI | 需要 AI 理解文档内容 | AI 自动读取并分析文档 |
| 让 AI 整理对话生成会议纪要 | 会议记录、日报周报 | 自动沉淀到飞书文档 |
| 让 AI 操作多维表格 | 数据整理、报表生成 | 自动创建/更新表格数据 |

---

## 行动清单

### 立即可做（今天）

- [ ] 在飞书开放平台创建企业自建应用
- [ ] 获取 App ID 和 App Secret
- [ ] 添加基础权限（文档读取、消息发送）

### 短期实践（本周）

- [ ] 在你使用的 AI Agent 中配置飞书 MCP
- [ ] 尝试让 AI 读取一篇飞书文档
- [ ] 尝试让 AI 创建一个多维表格并插入数据

### 长期提升（持续）

- [ ] 探索更多自动化场景（会议纪要、日报周报）
- [ ] 结合其他 MCP（如 GitHub MCP）构建工作流
- [ ] 关注飞书 MCP 的功能更新

---

## 金句摘录

> "本质上 AI 调用飞书 MCP 做某件事情，背后是飞书 MCP 调用飞书的 API 来实现的。"

> "这些功能都可以让 AI 自己实现，你只需要跟他提要求即可。"

---

## 延伸阅读

- [飞书 OpenAPI MCP 接入指南（官方）](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/mcp_integration/mcp_installation)
- [飞书开放平台](https://open.feishu.cn/)
- [MCP 协议官方文档](https://modelcontextprotocol.io/)
